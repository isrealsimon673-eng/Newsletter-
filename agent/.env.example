# Example environment variables for the agent
# Copy to `.env` and fill values as needed

# OPENAI_API_KEY for LLM mode
OPENAI_API_KEY=

# AGENT mode: 'rule' or 'llm'
AGENT_MODE=rule

# Optional Slack incoming webhook URL to post summaries
SLACK_WEBHOOK_URL=
