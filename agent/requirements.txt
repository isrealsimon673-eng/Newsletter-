# Minimal requirements for the scaffold.
# Add packages like `openai`, `requests`, or `rich` as you integrate external services.
openai>=1.0.0
requests>=2.0.0
pytest>=7.0.0
pytest-cov>=4.0.0
flake8>=5.0.0
gspread>=5.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
